const allowed=new Set(['discover','products','pins','experiments','analytics','recommendations','imports','ml','settings','logs']);
const labels:Record<string,string>={discover:'Discover',products:'Products',pins:'Pins',experiments:'Experiments',analytics:'Analytics',recommendations:'Recommendations',imports:'Imports',ml:'Machine Learning',settings:'Settings',logs:'Logs'};
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;if(!allowed.has(section)) return <><h1>Não encontrado</h1><p className="muted">Seção inexistente.</p></>;return <><span className="badge">FASE 1</span><h1>{labels[section]}</h1><div className="panel"><h2>Estrutura preparada</h2><p className="muted">Esta seção será implementada nas próximas fases. Nenhuma API ou dado externo fictício foi conectado.</p></div></>}
